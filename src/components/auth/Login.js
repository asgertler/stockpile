import React, { useRef } from 'react'
import { useHistory } from 'react-router-dom'
import { Row, Col } from 'antd'

import './auth.sass'

export const Login = (props) => {
    return (
        <Row justify='center' align='middle' id='loginContainer'>
            Hello world!
        </Row>
    )
}